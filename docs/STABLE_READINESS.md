# Stable Readiness

This adapter remains alpha until both release trust and Claude-native parity are
green. Hecate can already replace the package-manager adapter path with these
Go binaries; stable means the standalone adapter is ready for operators to
depend on directly.

## Required Gate Before `v0.1.0`

- `make release-check` passes on the release commit.
- `make real-cli-smoke` passes on a prepared machine with an authenticated
  Claude Code CLI.
- The Hecate ACP adapter release-binary smoke passes after Hecate is pinned to
  the candidate release.
- Release archives have checksums and GitHub artifact attestations generated by
  the tag workflow.
- The parity matrix below has no blocking `manual gap` rows.

## Parity Matrix

| Surface                    | Current state                                                                                                                                                   | Automated coverage                                                                                  | Stable gate                                                                                         |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| ACP initialize             | Adapter metadata, auth/logout, MCP, prompt, and stable session capabilities are advertised.                                                                     | Unit tests plus shared kit initialize conformance.                                                   | Covered.                                                                                            |
| Auth and logout            | ACP `authenticate` maps to `claude /login`; ACP `logout` maps to `claude auth logout`.                                                                          | Unit tests, fake bridge tests, and opt-in real CLI smoke.                                            | Cover at least one real authenticated local-login cycle before stable.                               |
| Session lifecycle          | `session/new`, `session/list`, `session/load`, `session/resume`, `session/fork`, `session/close`, and `session/delete` work for command-backed sessions.       | Unit tests, portable upstream parity checks, and opt-in real CLI list/load smoke.                     | Manually verify Claude `--session-id` / `--resume` behavior across adapter process restarts.         |
| Prompt streaming           | Claude stream JSON maps to ACP assistant text, thinking, usage, stop reasons, and tool updates.                                                                 | Source-shaped parser fixtures, command bridge tests, and opt-in real CLI prompt/tool smoke.           | Covered for real shell/file update flow; keep adding source-shaped fixtures as Claude Code evolves.  |
| Config selectors           | Static model, effort, and permission-mode selectors are exposed and updateable.                                                                                 | Unit tests and portable selector parity checks.                                                      | Decide whether runtime discovery is required or document static selectors as the contract.           |
| MCP handoff                | ACP stdio/HTTP MCP server config maps to Claude `--mcp-config` with strict config enabled.                                                                      | Unit tests, Hecate release-binary smoke, and opt-in real CLI stdio MCP echo-tool smoke.               | Add deeper provider MCP lifecycle and approval-elicitations coverage before stable.                  |
| Permissions and eliciting  | Parsed permission requests and denied/rejected/blocked provider tool results map to ACP permission/tool state; elicitation remains a deeper provider edge case. | Source-shaped parser fixtures, grant/approval tests, Hecate release-binary smoke, and opt-in real CLI permission auto-approval. | Add manual coverage for AskUserQuestion/MCP elicitation and late permission replies after cancel.    |
| Cancellation               | ACP cancel terminates active command-backed prompts and drops post-cancel stream chunks.                                                                        | Unit tests, command bridge tests, portable parity checks, Hecate release-binary smoke, and opt-in real CLI cancellation smoke. | Add real CLI checks for wedged-runtime forced cancellation; normal cancellation/no double-settle is covered. |
| Release artifacts          | GoReleaser archives are checksum-verified and provenance-attested by the tag workflow.                                                                          | Release workflow doc test and `make release-check`; release verification uses `gh attestation verify`. | Covered once the first attested release is published and verified.                                  |
| Hecate integration         | Hecate bundles released binaries and runs release-binary smoke against them.                                                                                    | Hecate `just test-acp-release-smoke`.                                                                | Covered for every Hecate adapter pin bump.                                                          |

## Stable Decision

Cut `v0.1.0` only after the unresolved stable gates are either implemented and
tested or explicitly documented as non-goals for the stable adapter contract.
